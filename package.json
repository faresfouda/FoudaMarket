{
  "name": "fouda-market",
  "version": "1.0.0",
  "description": "Fouda Market Flutter Web App",
  "scripts": {
    "build": "flutter build web --release",
    "dev": "flutter run -d chrome",
    "clean": "flutter clean",
    "build:verify": "npm run build && echo 'Build completed successfully'",
    "deploy:check": "echo 'Checking deployment status...'"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "google-auth-library": "^9.0.0"
  }
}
